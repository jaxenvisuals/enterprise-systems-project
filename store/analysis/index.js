export const state = () => {
  return {
    activeSelection: null,
  }
}

export const getters = {}

export const mutations = {
  setActiveSelection(state, val) {
    state.activeSelection = val
  },
}

export const actions = {}
